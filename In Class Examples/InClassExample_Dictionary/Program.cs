using System;

namespace InClassExample_Dictionary
{
    class Program
    {
        static void Main(string[] args)
        {
            Dictionary<string>, <double> new Dictionary<string>
            /*students.Add("1", 3.0);
            students.Add("1", 4.0);
            students.Add("1", 2.0);

            Console.WriteLine("Do you wnat to add another student?");

            if (answer.ToLower()[0] == 'y')
            {
                Console.WriteLine("what is the stiduent's id?");
                string id = Console.ReadLine();

                Console.WriteLine("");
                double gpa = Convert.ToDouble(Console.ReadLine());

                if (students.ContainsKey(id) == true)
                {
                    Console.WriteLine("Already contains that key, we will update");
                    students[id] = gpa;
                }
                else
                {
                    stidents.Add(id, gpa);
                }
            }
            foreach (string id in students.keys)
            {
                Console.WriteLine(id);
            }
            Console.WriteLine("Whose GPA do you want to see?");
            string searchID = Console.ReadLine();

            if (students.ContainsKey(searchID) == true)
            {
                Console.WriteLine($" ");
            }
            else
            {
                Console.WriteLine();*/
            }
    }
}
